# 5月22日

React作成

```
const Title =()=>{
    return(
        <div>
        </div>
    );
};

export default Title;
```
これが雛形になるとのこと

まずtopページに全て集約されているので、それぞれをtoppageコンポーネントで読み込む

そのためにexportとimportの両方を書いとく
